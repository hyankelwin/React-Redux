import React, { Component } from 'react';
import { RepositoriesContainer } from '../../containers';

class Repositories extends Component {
  render() {
    return (
      <RepositoriesContainer />
    );
  }
}

export default Repositories;
